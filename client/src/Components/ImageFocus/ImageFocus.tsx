import React from "react";

// Styles
import styles from "./ImageFocus.module.scss";

const ImageFocus: React.FC = () => {
  return (
    <div className={styles.imageFocus}>
      <div className={styles.container}></div>
    </div>
  );
};

export default ImageFocus;
