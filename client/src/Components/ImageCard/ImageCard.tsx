import React from "react";

// Styling
import classes from "./ImageCard.module.scss";

const ImageCard: React.FC = () => {
  return <div className={classes.imageCard}></div>;
};

export default ImageCard;
